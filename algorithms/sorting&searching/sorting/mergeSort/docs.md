#What is Merge Sort?
```
Merge Sort is a divide-and-conquer algorithm that works by
    recursively splitting the list into smaller sublists until
        each sublist has only one element. Then, it merges the sublist
            until there is only one sorted list remaining
```